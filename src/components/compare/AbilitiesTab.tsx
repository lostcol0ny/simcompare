import type { Report } from '@/lib/types'
export function AbilitiesTab({ reports: _ }: { reports: Report[] }) {
  return <div className="p-4 text-text-muted text-sm">Abilities tab — coming soon</div>
}
