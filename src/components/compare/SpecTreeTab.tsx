import type { Report } from '@/lib/types'
export function SpecTreeTab({ reports: _ }: { reports: Report[] }) {
  return <div className="p-4 text-text-muted text-sm">Spec Tree tab — coming soon</div>
}
